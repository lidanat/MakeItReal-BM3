let sampleArray = ['Lidana'];
console.log('Initial Array: ' , sampleArray);

//Insert positions in the array
sampleArray.push('Abigail');
sampleArray.push('Danny');
sampleArray.push('Martha');

for(let i = 0; i < sampleArray.length; i++){
    console.log('Modified Array: ' , sampleArray[i])
}


